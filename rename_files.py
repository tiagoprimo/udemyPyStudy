import os

def rename_files(): #function creted
    #(1) get file names from a folder
    file_list = os.listdir("../udemystudy/images")
    print(file_list)
    #(2) for each file, rename the filename



rename_files() #function called